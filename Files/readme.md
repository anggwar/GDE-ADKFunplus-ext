This files here contains the agents and .env files in case yours have problem.

You should follow the instructions first, before copying this agents file.

If you need it, download the .py files according to your step, then rename it to agent.py in code editor or from cloudshell to your agent folder destination.
you can use `adk web` to run te agent
